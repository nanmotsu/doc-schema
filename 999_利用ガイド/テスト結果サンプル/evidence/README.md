# evidence フォルダ

テスト実行後のエビデンス（スクリーンショット等）を保存するフォルダです。

- YAML では `evidence.screenshot` を使用します。
- 単一画像は文字列、複数画像は配列で保持します。
- グリッドで Ctrl+V 保存した画像は `ステップID_タイムスタンプ_UUID.png` 形式で追加されます。
- 手動で配置する場合は `TEST-001-TC001-S01.png` のようなステップID準拠の命名を推奨します。
